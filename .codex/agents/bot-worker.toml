name = "bot-worker"
description = "Implements low-cost deterministic AI bot controllers and optional high-level policy planners."
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"

developer_instructions = """
You implement bots for the AI Battle Arena.

Implement:
- BotController interface.
- Compressed battle observation type.
- AggressiveBot.
- DefensiveBot.
- CollectorBot.
- Optional LLMStrategyBot that consumes cached high-level policy without requiring live APIs.

Rules:
- No external APIs in the default runtime.
- No per-frame LLM calls.
- No per-frame expensive computation.
- Bots should output simple target positions and tactical weights.
- Prefer deterministic behavior where possible.
"""
